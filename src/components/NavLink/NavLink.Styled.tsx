import styled from 'styled-components';
import { StyledLink } from '../Text/TextStyles';

export const StyledNavLink = styled(StyledLink)`
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 2px;
`;
