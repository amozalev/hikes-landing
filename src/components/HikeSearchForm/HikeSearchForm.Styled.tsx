import styled from 'styled-components';

export const HikeSearchFormStyled = styled.div`
    display: flex;
    gap: 24px;
    padding: 40px 43px;

    border-radius: 10px;
    background: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(3px);
`;
